package org.example.store.model;
import lombok.*;
import org.antlr.v4.runtime.misc.NotNull;


/*@Entity
@Builder
@Table(name = "terminal")
@ToString
@Getter
@Setter
@AllArgsConstructor

@NoArgsConstructor*/
public class Terminal {
/*    public Terminal(PointTerminal location, Money money, double speed) {
        this.location = location;
        this.money = money;
        this.speed = speed;
    }

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private int id;

    @ManyToOne
    @JoinColumn(name = "location_id", referencedColumnName = "id")
    private PointTerminal location;

    @ManyToOne
    @JoinColumn(name = "money_id", referencedColumnName = "id")
    private Money money;

    @Column(name="speed")
    private double speed;*/

}